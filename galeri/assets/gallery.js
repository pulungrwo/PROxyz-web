(()=>{const PAGE_SIZE=24;const token=document.body.dataset.galleryToken;const gallery=document.getElementById('gallery');const empty=document.getElementById('empty');const search=document.getElementById('search');const period=document.getElementById('period');const countEl=document.getElementById('gallery-count');const updatedEl=document.getElementById('gallery-updated');const resultCount=document.getElementById('result-count');const pageRange=document.getElementById('page-range');const pagination=document.getElementById('pagination');const pagePrev=document.getElementById('page-prev');const pageNext=document.getElementById('page-next');const pageNumbers=document.getElementById('page-numbers');const footerPage=document.getElementById('footer-page');const viewer=document.getElementById('viewer');const vImg=document.getElementById('viewer-image');const vCaption=document.getElementById('viewer-caption');const vDate=document.getElementById('viewer-date');const vOrigin=document.getElementById('viewer-origin');const vOriginal=document.getElementById('viewer-original');const vDownload=document.getElementById('viewer-download');let photos=[];let currentPage=1;let activePhoto=null;const fmt=new Intl.DateTimeFormat('id-ID',{dateStyle:'medium',timeStyle:'short',timeZone:'Asia/Jakarta'});function matchesPeriod(ts,type){if(type==='all')return true;const d=new Date(ts),n=new Date();const dKey=d.toLocaleDateString('en-CA',{timeZone:'Asia/Jakarta'}),nKey=n.toLocaleDateString('en-CA',{timeZone:'Asia/Jakarta'});if(type==='today')return dKey===nKey;const dp=dKey.split('-'),np=nKey.split('-');if(type==='month')return dp[0]===np[0]&&dp[1]===np[1];return dp[0]===np[0]}function filteredRows(){const q=search.value.trim().toLowerCase();return photos.filter(p=>matchesPeriod(p.createdAt,period.value)&&(!q||String(p.caption||'').toLowerCase().includes(q)||String(p.uploaderName||'').toLowerCase().includes(q)||String(p.id||'').toLowerCase().includes(q)||String(p.originGroupName||'').toLowerCase().includes(q)))}function cardFor(p,index){const b=document.createElement('button');b.className='card';b.type='button';const img=document.createElement('img');img.loading=index<4?'eager':'lazy';img.decoding='async';img.src=p.publicUrl;img.alt=p.caption||p.id;const badge=document.createElement('span');badge.className='badge';badge.textContent=p.id;b.append(img,badge);b.addEventListener('click',()=>openViewer(p));return b}function paginationItems(totalPages,page){if(totalPages<=5)return Array.from({length:totalPages},(_,i)=>i+1);const items=[1];if(page>3)items.push('…');const from=Math.max(2,page-1),to=Math.min(totalPages-1,page+1);for(let i=from;i<=to;i++)items.push(i);if(page<totalPages-2)items.push('…');items.push(totalPages);return items}function renderPagination(totalRows,totalPages){pagination.hidden=totalPages<=1;pagePrev.disabled=currentPage<=1;pageNext.disabled=currentPage>=totalPages;pageNumbers.innerHTML='';for(const item of paginationItems(totalPages,currentPage)){if(item==='…'){const s=document.createElement('span');s.className='page-ellipsis';s.textContent='…';pageNumbers.appendChild(s);continue}const b=document.createElement('button');b.type='button';b.className='page-number'+(item===currentPage?' active':'');b.textContent=String(item);b.setAttribute('aria-label','Halaman '+item);if(item===currentPage)b.setAttribute('aria-current','page');b.onclick=()=>goToPage(item);pageNumbers.appendChild(b)}footerPage.textContent=totalPages>0?'Halaman '+currentPage+' / '+totalPages:''}function goToPage(page){const rows=filteredRows();const totalPages=Math.max(1,Math.ceil(rows.length/PAGE_SIZE));currentPage=Math.min(Math.max(1,page),totalPages);render({scroll:true})}function render(options={}){const rows=filteredRows();const totalPages=Math.max(1,Math.ceil(rows.length/PAGE_SIZE));if(currentPage>totalPages)currentPage=totalPages;const start=(currentPage-1)*PAGE_SIZE;const visible=rows.slice(start,start+PAGE_SIZE);gallery.innerHTML='';const groups=new Map();for(const p of visible){const key=p.batchId||p.id;if(!groups.has(key))groups.set(key,[]);groups.get(key).push(p)}let imageIndex=0;for(const batch of groups.values()){const first=batch[0];const section=document.createElement('section');section.className='batch-section';const head=document.createElement('div');head.className='batch-head';const title=document.createElement('h2');title.textContent=first.caption||'Tanpa keterangan';const info=document.createElement('span');info.textContent=batch.length+' foto di halaman ini · '+fmt.format(new Date(first.createdAt));head.append(title,info);const grid=document.createElement('div');grid.className='batch-grid';for(const p of batch)grid.appendChild(cardFor(p,imageIndex++));section.append(head,grid);gallery.appendChild(section)}empty.hidden=rows.length!==0;resultCount.textContent=rows.length+' foto';pageRange.textContent=rows.length?((start+1)+'–'+Math.min(start+PAGE_SIZE,rows.length)+' dari '+rows.length):'';renderPagination(rows.length,totalPages);if(options.scroll)window.scrollTo({top:document.querySelector('.toolbar').offsetTop-12,behavior:'smooth'})}function openViewer(p){activePhoto=p;vImg.src=p.publicUrl;vImg.alt=p.caption||p.id;vCaption.textContent=(p.caption||'Tanpa keterangan')+' · '+p.id;vDate.textContent=fmt.format(new Date(p.createdAt))+(p.uploaderName?' · '+p.uploaderName:'');vOrigin.textContent=p.originGroupName?'Sumber: '+p.originGroupName:'';vOriginal.href=p.publicUrl;vDownload.disabled=false;vDownload.textContent='↓ Download';viewer.showModal()}async function downloadActivePhoto(){if(!activePhoto)return;const photo=activePhoto;const originalText=vDownload.textContent;vDownload.disabled=true;vDownload.textContent='Menyiapkan…';try{const response=await fetch(photo.publicUrl,{mode:'cors'});if(!response.ok)throw new Error('download');const blob=await response.blob();const ext=(String(photo.mimeType||'').split('/')[1]||'jpg').replace('jpeg','jpg');const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=(photo.id||'FOTO')+'.'+ext;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000)}catch(err){const a=document.createElement('a');a.href=photo.publicUrl;a.target='_blank';a.rel='noopener';a.download=(photo.id||'FOTO')+'.jpg';document.body.appendChild(a);a.click();a.remove()}finally{vDownload.disabled=false;vDownload.textContent=originalText}}document.getElementById('close-viewer').onclick=()=>viewer.close();viewer.addEventListener('click',e=>{if(e.target===viewer)viewer.close()});viewer.addEventListener('close',()=>{vImg.removeAttribute('src');activePhoto=null});vDownload.addEventListener('click',downloadActivePhoto);pagePrev.onclick=()=>goToPage(currentPage-1);pageNext.onclick=()=>goToPage(currentPage+1);search.addEventListener('input',()=>{currentPage=1;render()});period.addEventListener('change',()=>{currentPage=1;render()});fetch('../../data/galeri/'+encodeURIComponent(token)+'.json',{cache:'no-store'}).then(r=>{if(!r.ok)throw new Error('Data galeri tidak ditemukan');return r.json()}).then(data=>{photos=Array.isArray(data.photos)?data.photos:[];countEl.textContent=photos.length+' foto';updatedEl.textContent='diperbarui '+fmt.format(new Date(data.updatedAt||Date.now()));render()}).catch(err=>{countEl.textContent='Galeri belum tersedia';updatedEl.textContent='';resultCount.textContent='';pageRange.textContent='';empty.textContent=err.message;empty.hidden=false})})();